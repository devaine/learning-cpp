{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Program",
            "program": "${fileDirname}/main.exe",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}