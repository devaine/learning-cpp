/*
    To save you some time, you can find the syntax for launch.json for debugging here:
    https://code.visualstudio.com/docs/editor/variables-reference
*/
{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Program",
            "program": "${fileDirname}/main.exe",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}